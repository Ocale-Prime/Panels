#include <windows.h>
using namespace std;

namespace cr1
{
    template<typename Toy>
    static template<typename joid>
    void create_memory(joid loader)
    {
        LOCALVMWARE vmware_tools;
        vmware_tools.isofunction = sizeof(vmware_graphics.vmloader.graphics::loader());
        loader.tools = vmware_tools.load_iso();
    };

  return 0;
};
