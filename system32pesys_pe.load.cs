using System;
using System.Formats.Asn1;

namespace system
{
    class local_init
    {
        protected static void local()
        {
            var system_load = new function();
            system_load.init_enterprises = sizeof(system_load.funct_cpu);
        }
    }

  class return_init : local_init
  {
      protected static void ld()
      {
          Console.WriteLine("cpup")
      }
  }
}
